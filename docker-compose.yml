services:
  bot:
    build: .
    container_name: wechat-reply-flow
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: 3000
      WECHAT_PATH: /wechat
    ports:
      - "3000:3000"
    restart: unless-stopped
